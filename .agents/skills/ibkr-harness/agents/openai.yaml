interface:
  display_name: "IBKR Harness"
  short_description: "Inspect live IBKR harness contracts safely"
  brand_color: "#2563EB"
  default_prompt: "Use $ibkr-harness to inspect the account, rulebook, data quality, or a preview-only order contract safely."

dependencies:
  tools:
    - type: "mcp"
      value: "ibkr"
      description: "Local read-only IBKR MCP server exposed by this repo's CLI."

policy:
  allow_implicit_invocation: true
