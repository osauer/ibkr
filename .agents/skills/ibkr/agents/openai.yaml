interface:
  display_name: "IBKR Project"
  short_description: "Read IBKR data and safety contracts"
  brand_color: "#2563EB"
  default_prompt: "Use $ibkr to check account, positions, quote freshness, or a preview-only order contract safely."

dependencies:
  tools:
    - type: "mcp"
      value: "ibkr"
      description: "Local read-only IBKR MCP server exposed by this repo's CLI."

policy:
  allow_implicit_invocation: true
