{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "command -v jq >/dev/null 2>&1 || { echo 'ibkr plugin: jq missing on PATH; failing closed on trading verbs' >&2; exit 2; }; jq -e '.tool_input.command | test(\"(^|[/\\\\s])ibkr\\\\s+(order|trade|cancel|submit|place)\\\\b\")' >/dev/null && exit 2 || exit 0"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh\""
          }
        ]
      }
    ]
  }
}
